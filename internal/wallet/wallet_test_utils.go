package wallet

import (
	"time"
)

// Shared test variables and mock data
var (
	testUserID   = 1
	testToUserID = 2
	testAmount   = 50.0
	testEmail    = "user1@example.com"
	now          = time.Now()
)
